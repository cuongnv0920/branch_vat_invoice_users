const validatorKeys = {
  FILE_SIZE_PDF: 8 * 1024 * 1024,
  SUPPORTED_FORMATS_PDF: ["application/pdf"],

  FILE_SIZE_XML: 2 * 1024 * 1024,
  SUPPORTED_FORMATS_XML: ["text/xml"],
};

export default validatorKeys;
